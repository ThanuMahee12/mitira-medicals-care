import React, { Component, memo } from 'react';

class Register extends Component {
    render() {
        return (
            <div>
                Register
            </div>
        );
    }
}

export default memo(Register);
